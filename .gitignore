# Build artefacts
build/
dist/
*.o
*.lo
*.la
*.a
*.so
*.so.*
.deps/
.libs/

# Autotools: generated by `./configure` or `make` (NOT shipped in tarball)
# Anything in the upstream tarball IS source and must be tracked.
config.h
config.h.in
config.h.in~
config.log
config.status
config.cache
libtool
libtool.la
autom4te.cache/
.deps/
.libs/
stamp-h1
stamp-h2
# `Makefile` is the file generated by `./configure` (substitute of
# Makefile.in). `Makefile.in` is the input and is tracked above.
Makefile

# ICU-specific build outputs (under upstream/icu/)
upstream/icu/source/data/out/
upstream/icu/source/data/icu/
upstream/icu/source/bin/
upstream/icu/source/lib/
upstream/icu/source/config/icucross.mk
upstream/icu/source/config/Makefile.inc
upstream/icu/source/config.status

# ICU-specific build artefacts
upstream/icu/source/data/out/
upstream/icu/source/data/icu/
upstream/icu/bin/
upstream/icu/lib/
upstream/icu/config/icucross.mk
upstream/icu/config/Makefile.inc
upstream/icu/config.status

# CI artefacts
.github/release-output/

# Editor / OS
*.swp
*~
.DS_Store
*.tmp
upstream/dwdiff/dwdiff
upstream/dwdiff/dwfilter
/build/
